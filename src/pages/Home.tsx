import { Section, Container } from 'components';

const Home: React.FC = () => {
  return (
    <Section full extraClass="section-hero">
      <Container>
        <h1>Main Page</h1>
      </Container>
    </Section>
  );
};

export default Home;
